# 21st.dev Integration — TASK Checklist

> Checklist task sprint. Spec/AC: `docs/21st-integration-plan.md` (autoritatif). PRD/analisis:
> `docs/PRD-21ST-DEV-INTEGRATION.md`.
> Status terakhir disinkronkan: 2026-09-24 (Phase 0 ✅ + Task 1.1 ✅ — eksekusi Sprint 1 dimulai).

## Phase 0 — Discovery & Baseline
- [x] **`git status` + baseline tests** — working tree BERSIH (2026-09-24). Baseline: `build:fast` 49 page ✓ (warn GitHub 403/404 build-time non-fatal, pre-existing); `test` **744/744 (66 file)** ✓; `astro check` **109 errors (0 baru — baseline Motion-II)** ✓; `lint` (biome check .) **739 errors pre-existing** → snapshot `/tmp/opencode/lint-baseline.txt`; aturan: file tersentuh wajib clean, error pre-existing og/[...route].ts/rss.xml.ts/__tests__/work/index.astro diabaikan. e2e baseline: server preview :4321 up (reuse), 15 spec — full suite dijadwalkan Phase Final.
- [x] **Baca file tersentuh** — via subagent: Button.tsx (pola cva+forwardRef+cn, `text-bg-primary` pakai token), InteractionButton.tsx (`text-white` L25, `text-green/red-500` L162/189), ContactForm.tsx (3 class chain L73/95/117, error `text-red-500`/`role=alert`, web3forms access key L7 — `|| "YOUR_ACCESS_KEY_HERE"`), SkillsExplorer.tsx (input L54–60 tanpa label; empty-state L132–139), TestimonialCarousel.tsx (8 hex L34–95; autoPlay prop MATI — tidak ada setTimeout; tanpa aria-live), AssistantBot.tsx (`aria-modal="false"` L128, 6× `text-[#0c0d0b]` L34/116/138/209/242/289, tanpa focus trap/restore), GalleryGrid.tsx (modal tanpa role dialog/aria-modal L571–588, tanpa focus trap; `export GALLERY_EXPERIMENTS` L402–404; empty-state filter TIDAK ADA; `text-white` L599), CommandPalette.tsx (pola trap L107–128 + prevFocusRef restore L74/141–149), gallery.astro (L9/L26 "25" stale, L32 nested container — diverifikasi manual), theme.css (3 blok theme; ADA `--color-brand-text`=on-brand light #5d6b54/dark #7a8c6f; TIDAK ADA token error/on-accent; `--color-brand-warm`=#C17F59). Header.astro + ThemeToggle.astro dibaca manual (pola inline script).
- [x] **Konsultasi katalog 21st gratis** — dibaca (tanpa CLI install, D1): `https://21st.dev/llms.txt`, `.../s/navigation.md` (navbar-02 → sheet menu), `.../s/form.md`, `.../s/testimonials.md`, `@shadcnui-blocks/components/navbar-02.md`, `@shadcn/components/input.md`, `@shadcn/components/dialog.md`. **Catatan**: halaman `.md` komponen hanya berisi katalog+install (kode butuh key/retrieval berbayar) → pola diadaptasi dari konvensi repo + shadcn anatomy.

## Sprint 1 — Δ1 Mobile Site Navigation (G1)
- [x] **1.1 Desain & keputusan** — markup/state final: (a) **trigger burger** button `lg:hidden` + `aria-expanded`/`aria-controls="mobile-nav"`, label dinamis "Menu"/"Tutup menu", icon lucide Menu/X inline SVG (konsisten dgn Cari); (b) **panel** = sibling SETELAH `</header>` (bukan child — header punya `backdrop-blur-md` = containing-block untuk fixed descendants; fixed inset-0 di dalamnya salah); wrapper `data-mobile-nav` `lg:hidden` berisi backdrop (`fixed inset-0 z-[60] bg-black/60 backdrop-blur-sm`) + sheet (`fixed inset-y-0 right-0 z-[70] w-72 bg-bg-primary border-l border-border shadow-2xl`, role=dialog aria-modal=true aria-label="Menu navigasi", row header dgn tombol tutup X + 6 nav links dari `navLinks` dgn `aria-current`); z 60/70 → header z-50 di atas konten, di bawah chrome global 9996+ ✓; (c) **state**: class `is-open` di container via inline script (pola ThemeToggle, 0 React root) — buka/tutup via CSS transition (backdrop opacity, sheet translate-x — compositor-only tier S/A), `visibility` toggle; reduced-motion → `@media (prefers-reduced-motion: reduce)` matikan transition (presen instan, tanpa JS guard); (d) **a11y**: Esc close + focus return ke trigger, close on backdrop click + link click, body scroll-lock (simpan prev overflow), `aria-expanded` sinkron; TAB trap penuh = di luar scope (Sprint 3 untuk React overlays); (e) **listener baru**: hanya click/keydown inline (0 scroll listener, 0 RAF); (f) urutan tombol mobile: [search icon][burger][HeaderTools][divider][ThemeToggle]. — *D4: disclosure inline-script, BUKAN island baru*
- [x] **1.2 Implementasi di Header.astro** — trigger `aria-expanded`/`aria-controls`, panel backdrop+sheet `lg:hidden`, 6 nav links + `aria-current`, inline script toggle/Esc/focus-return/scroll-lock, no new React root, no new scroll listener. — **IMPLEMENTED**: burger trigger (icon Menu/X swap via state `<html data-mobile-nav-open>`), panel = sibling setelah `</header>` (header `backdrop-blur-md` = containing-block utk fixed descendants — panel fixed inset-0 di dalamnya salah), backdrop z-[60] + sheet z-[70] (`role="dialog" aria-modal aria-label="Menu navigasi"`, tombol tutup + 6 links), CSS scoped transition backdrop opacity + sheet translateX (compositor S/A) + `@media (prefers-reduced-motion: reduce)` instant, inline script pola ThemeToggle (0 React root). Grep: 1× `client:` = pre-existing HeaderTools; 0 scroll listener; 0 hex DOM. Biome Header.astro CLEAN.
- [x] **1.3 E2E mobile nav** — **`e2e/mobile-nav.spec.ts` 6 test, 6/6 HIJAU** (server reuse :4321): (1) trigger membuka sheet dgn 6 link di /gallery, (2) klik "Observatory" → panel tutup + navigate ke /observatory (non-index ✓), (3) keyboard Enter buka/Esc tutup + **focus kembali ke trigger**, (4) backdrop click tutup, (5) /observatory juga punya trigger+panel, (6) reduced-motion context buka/tutup tanpa animasi. — *6 test hijau terisolasi*

### Sprint 1 verifikasi
- [x] **VERIFIKASI Sprint 1** — `build:fast` ✓ (49 page), unit ✓ (744/744 — 0 unit baru: inline script dilindungi e2e, preseden ThemeToggle), e2e mobile-nav **6/6** ✓, `astro check` **109 (0 baru)** ✓, biome file tersentuh CLEAN ✓, grep: 0 React root baru / 0 scroll listener baru ✓. **`check-budget`: PRE-EXISTING RED di HEAD** (654.7KB gzip > 293KB budget, byte-identik dgn HEAD via A/B stash build; delta perubahan 21st = **0 KB JS** — perubahan ini murni HTML+CSS+inline script) → dicatat di Blocker/Deviasi; bukan blocker sprint.

## Sprint 2 — Δ2 Form primitives + button tokens (G3 + G5)
- [x] **2.1 Form primitives** — `src/components/atoms/Input.tsx`, `Textarea.tsx`, `Label.tsx` + `FormField.tsx`: pola cva+forwardRef ala Button.tsx, token rgb, focus-visible ring accent, state error (`aria-invalid` + `border-danger`/`ring-danger/40`), unit test per atom. — **IMPLEMENTED**: (1) token baru `--color-danger` (#b42318 light / #f2b8b5 dark) + `-rgb` di 3 blok theme.css + alias legacy `--danger`/`--danger-rgb` + mapping Tailwind `danger` (warna DOM tetap 100% token, D3 ✓); (2) `Input.tsx` (cva variant default/ghost × size md/lg; `error` flag convenience → set `aria-invalid="true"` — visual style dikemudi CSP `aria-invalid:`; `Omit<…, "size">` utk resolusi konflik native `size?: number` (ts 2320/2322), API tetap `size="md"|"lg"`), `Textarea.tsx` (sama, `min-h-[88px]` + `resize-y` tanpa fixed h), `Label.tsx` (forwardRef + `srOnly`; biome-ignore `noLabelWithoutControl` — primitif reusable, assosiasi dari consumer props, preseden repo), `FormField.tsx` (id/label/srOnly/error/hint; `space-y-1` = 4px = parity mb-1/mt-1 ContactForm; error `<p id={id}-error role="alert" class="mt-1 text-sm text-danger">`, hint id `${id}-hint` — siap di-`aria-describedby`); (3) 4 test file baru (29 test: render/variants/error aria/disabled/forwardRef/cn-merge/srOnly/ref). **Verifikasi**: unit **773/773** (70 file, +29) ✓; biome 8 file atom + theme.css + tailwind.config CLEAN (theme.css shadow rgba diformat biome — murni kosmetik pre-existing); `astro check` **109 (0 baru — Input Omit fix)** ✓.
- [x] **2.2 Retheme ContactForm + SkillsExplorer** — ganti 3 field ContactForm + input SkillsExplorer ke primitif; perilaku form TIDAK berubah (RHF/zod/web3forms/honeypot/InteractionButton/toast); fix fallback access key diam → dev warn terdokumentasi; 0 duplikasi class-chain tersisa (grep). — **IMPLEMENTED**: (1) ContactForm 3 field → `FormField`+`Input`/`Textarea` (id cf-name/-email/-message → error id `${id}-error` = sama dgn konstanta lama; `error={!!errors.x}` → `aria-invalid` CSP `aria-invalid:border-danger`/`ring-danger/40` menggantikan `border-red-500`; `aria-describedby` tetap eksplisit; `transition-all duration-200 focus-visible:border-transparent` dipertahankan via className — parity visual fokus; `resize-none`/`rows={4}` dipertahankan); honeypot/RHF/zodResolver/web3forms/InteractionButton/toast/reset TIDAK disentuh; (2) dev warn access key: `if (import.meta.env.DEV && !PUBLIC_WEB3FORMS_ACCESS_KEY) console.error("[ContactForm] …")` — tanpa ubah transport, guard DEV agar prod console bersih; (3) SkillsExplorer input → `Input` (twMerge dedupe: `py-3`/`bg-bg-secondary/50`/`placeholder:…/30`/`focus:border-brand` override menang, text-sm/rounded-lg dasar cocok) + `Label htmlFor="skills-search" srOnly` (nama aksesibel baru; placeholder tetap; +ring accent default di focus — a11y improvement terdokumentasi); (4) **e2e baru `e2e/contact.spec.ts` 4 test** (render 3 field berlabel + honeypot; submit kosong → 3 `role=alert` pesan zod + aria-invalid; submit ulang clear error field valid — *pelajaran: mode RHF onSubmit + trigger manual = error bertahan saat mengetik, hilang hanya saat validasi ulang — perilaku PRE-EXISTING, test awal saya salah asumsi re-validasi-onChange*; SkillsExplorer search bernama + filter). **Verifikasi**: build:fast ✓ 49 page; e2e contact **4/4** + mobile-nav regression **6/6** (9 passed/1 failed awal = salah asersi saya, 0 regresi form); `astro check` **109 (0 baru)** ✓; biome 3 file CLEAN ✓; grep: `red-500`/hex/fragment class-chain lama di 2 file = **0** ✓.
- [x] **2.3 Konsolidasi warna tombol DOM** — token `--on-accent`/`--on-brand` (dark+light) di theme.css; ganti `text-[#0c0d0b]` (AssistantBot, GalleryGrid) & `text-white` (InteractionButton) & konsistenkan Button; grep hex DOM 4 file = 0; pengecualian canvas/SVG constants dicatat. — **IMPLEMENTED**: (1) token baru `--color-on-accent`/`--color-on-brand` (+`-rgb`) di 3 blok theme.css + alias legacy `--on-accent`/`--on-brand` + mapping Tailwind `on-accent`/`on-brand`; nilai = kontras-optimal per tema: light `#fafaf8` (utk bg-brand/accent gelap #5d6b54) · dark `#0e0f0c` (utk bg-brand mid #7a8c6f) — pola yang sama dgn Button primary (`text-bg-primary`); sekaligus **memperbaiki kontras 2.9:1 → ~6.2:1** (light AssistantBot chips & dark InteractionButton); (2) AssistantBot 6× `text-[#0c0d0b]`→`text-on-brand` (semua di `bg-brand` — satu konvensi bg-brand+on-brand); InteractionButton `text-white`→`text-on-brand` L25 + error state `text-red-500`→`text-danger` L189 (token yang sudah ada); Button primary `bg-accent text-bg-primary`→`bg-accent text-on-accent` (konsistenkan, visual IDENTIK — on-accent == surface-primary tiap tema); **GalleryGrid L599 `text-white` DIKEEP** (chip ikon di atas gradient eksperimen `from-amber-400…` dll — dekoratif, tak ada on-token universal) + **`text-green-500` L162 InteractionButton DIKEEP** (state sukses transient; belum ada token success) — keduanya dicatat exception. **Verifikasi**: grep `text-[#`/hex/arbitrary di 4 file = **0**; biome A/B stash = **0 error baru** (9 pre-existing identik HEAD: noConfusingVoidType/useSemanticElements×2/useExhaustiveDependencies + org/format drift — tak disentuh); e2e assistant+micro+craft **19/19** ✓; unit **773/773** ✓; `astro check` **109 (0 baru)** ✓; build:fast ✓ 49 page.

### Sprint 2 verifikasi
- [x] **VERIFIKASI Sprint 2** — unit **773/773** (70 file, +29 baru) ✓; unit baru 29 (Input 8/Textarea 8/Label 5/FormField 8) + e2e contact baru **4/4** + regresi mobile-nav **6/6** + assistant/micro/craft **19/19** ✓ (server reuse :4321, build:fast pasca-token); `astro check` **109 (0 baru)** ✓; biome: file baru CLEAN + A/B stash konfirmasi **0 error baru** di file tersentuh (pre-existing: theme.css shadow format dibersihkan — kosmetik; 9 lint di AssistantBot/InteractionButton identik HEAD) ✓; visual dark+light: token on-brand/on-accent kontras-optimal (light #fafaf8 / dark #0e0f0c pada bg-brand/accent), perilaku form identik (mode RHF onSubmit + trigger manual — error bertahan saat mengetik, hilang saat validasi ulang; di-cover e2e contact) ✓; grep hex di 4 file tombol = 0, `red-500` di ContactForm = 0 ✓. **cacatan**: Delta visual kecil terdokumentasi — Input pakai text-sm (13.3→14px) + ring-offset-2 (focus), error `border-danger` (#b42318/#f2b8b5) menggantikan `border-red-500`, on-brand menaikkan kontras — semuanya sesuai kompas token & a11y.

## Sprint 3 — Δ3 Overlay focus + testimonials + gallery (G4 + G2 + G6)
- [x] **3.1 `useFocusTrap` shared** — `src/lib/useFocusTrap.ts` (pola CommandPalette: cycle Tab/Shift+Tab, initial focus, return focus, cleanup) + unit test 10/10; terapkan ke drawer AssistantBot (trap + `aria-modal="true"` + efek setTimeout focus lama dihapus → `initialFocus: "input[aria-label='Pesan ke assistant']"`, return focus ke FAB) + modal GalleryGrid (trap di backdrop `enabled: experiment != null`, initial focus = tombol tutup pertama); **CommandPalette TIDAK diubah**. Detail: scope trap = `active?.closest('[role="dialog"], dialog') ?? container` (dukung modal bertingkat — engine modal AssistantBot tdk ditarik ke drawer); listener `keydown` capture di window; rAF→fallback setTimeout; restore bila fokus di dalam kontainer ATAU `!container.isConnected` (React unmount cleanup jalan setelah DOM dilepas).
- [x] **3.2 TestimonialCarousel retheme + a11y** — semua hex DOM → token (`text-text-primary/secondary`, `bg-bg-tertiary`, `text-brand`, `hover:text-brand-warm`, `bg-brand-warm` active dot); `aria-live="polite"` + `aria-label="Testimoni i dari n"` + `aria-roledescription="carousel"`; auto-advance HIDUP (setInterval) + pause saat hover/focus/reduced-motion + tombol pause/play (`aria-pressed`); label Indonesia; dot `key={t.id}` (biome noArrayIndexKey). Unit 8/8.
- [x] **3.3 Gallery fixes** — `experimentCount = GALLERY_EXPERIMENTS.length` (27, sinkron registry) dipakai di description meta + paragraf `gallery.astro`; nested container dihapus; empty-state filter 0 hasil = `<output>` (live region — lihat cacatan lint) + tombol reset "Tampilkan semua (N)".

### Sprint 3 verifikasi
- [x] **VERIFIKASI Sprint 3** — unit **791/791** (72 file, +18: hook 10 + carousel 8) ✓; e2e keyboard: assistant **7/7** (3 trap baru: initial focus input + wrap Tab + Esc) + gallery **72/72** (1 trap baru: initial focus tombol tutup + wrap + Esc) + regresi contact **4/4** (1 flake paralel → hijau terisolasi) + mobile-nav **6/6** ✓; testimonial dark+light = token-only (hex 0) ✓; `build:fast` 49 page ✓; `astro check` **109 (0 baru)** ✓; biome: 7 file tersentuh CLEAN + A/B GalleryGrid/AssistantBot = **0 error baru** (5 pre-existing GalleryGrid dibiarkan; 3 pre-existing AssistantBot dibiarkan) ✓; grep gates: hex DOM 0 / framer-motion 0 / listener scroll+wheel baru 0 ✓.
- **cacatan lint**: (1) `role="dialog"` motion.div di modal GalleryGrid = lint baru `useSemanticElements` — **biome-ignore TIDAK berfungsi utk rule ini di JSX** (1.9.4: suppression diabaikan/"unused" walau format benar — diuji 2 bentuk). Solusi = konversi panel ke **native `<dialog open>`** (implicit role=dialog, biome justru merekomendasikannya; `motion.dialog` didukung; hook `useFocusTrap` sudah men-target selector `'[role="dialog"], dialog'`; kelas panel `m-0 p-0 border` menetralkan UA styling; `open` tetap ada saat exit AnimatePresence). (2) `role="status"` div empty-state = lint sama → `<output>` (preseden AGENTS.md lesson C3/CapabilityGenerator; `<p>`→`<span>` krn output hanya phrasing content). (3) **REG RESI: fixture `useFocusTrap.test.tsx` sempat kehilangan `ref={ref}` di `<dialog>` saat pass biome-format (konversi div→dialog) → ref tdk attach → 7/10 test merah**; tertangkap verifikasi & diperbaiki (10/10 hijau). Pelajaran: setelah rewrite JSX element (div→dialog), cek ulang `ref` masih terpasang.

## Phase Final — Full verification & docs sync
- [x] **Full build + budget** — `bun run build` (fetch-data + astro, **49 page**) ✓; `check-budget` **RED 657.3 KB gzip > 293 KB** = **DEV-1 pre-existing terdokumentasi** (delta 21st JS = 0; butuh dedup vendor di luar scope) — bukan blocker.
- [x] **Full suites** — `bun run test` **791/791 (72 file)** ✓; full `bunx playwright test` **176/178** + 1 flake contact (race `getByRole('alert')` saat paralel penuh) **hijau terisolasi 2×** = efektif 178/178 ✓; `astro check` **109 (0 baru)** ✓; biome A/B **0 error baru** (7 file tersentuh CLEAN; GalleryGrid 5 + AssistantBot 3 = pre-existing dibiarkan).
- [x] **Grep gate** — `text-[#`/`bg-[#` di file tersentuh = **0**; `framer-motion` = **0**; scroll/wheel `addEventListener` baru = **0**; import baru = hanya hook/form primitives yang direncanakan.
- [x] **MotionScore spot (opsional)** — `/` **B 57** (range B 56–58 ✓, listeners 26) · `/gallery` **S 85** ✓ · `/observatory` **A 76** ✓ — tidak turun.
- [x] **Docs sync** — baris ini: TASKS semua `- [x]` ✓; PRD §10 = narrative, kondisi 1–6 & 8–9 terpenuhi (7: `check-budget` = DEV-1 terdokumentasi); `prompt.txt` line 1 dirotasi ✓; AGENTS.md sprint log ditambah (section "Sprint 21st Integration (Δ1–Δ3)") ✓.
- [x] **Selesai total** — semua verifikasi lulus (satu-satunya item tidak penuh = `check-budget`, DEV-1 pre-existing di luar scope, tercatat). Output penutup diberikan: **"implementasi sprint integrasi 21st.dev sudah selesai secara keseluruhan"**.

## Blocker / Deviasi
- **DEV-1 (2026-09-24) budget JS pre-existing**: `node scripts/check-performance-budget.mjs` red di HEAD (`654.7 KB gzip > 293.0 KB budget`) — diverifikasi via A/B: `git stash` → build HEAD → ukur = byte-identik. Bukan dari 21st (delta JS = 0). Fix butuh dedup/chunk-split vendor (di luar scope Δ1–Δ3); dicatat untuk final report user. *checklist item "check-budget lulus" di VERIFIKASI Sprint 1 / Phase Final = tidak dapat dipenuhi tanpa fix di luar scope.*
- (Kosong selain di atas — catat di sini bila ada: API key 21st, keputusan user, discrepancy plan, dsb.)
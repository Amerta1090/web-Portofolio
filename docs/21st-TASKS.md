# 21st.dev Integration — TASK Checklist

> Checklist task sprint. Spec/AC: `docs/21st-integration-plan.md` (autoritatif). PRD/analisis:
> `docs/PRD-21ST-DEV-INTEGRATION.md`.
> Status terakhir disinkronkan: 2026-09-24 (Phase 0 ✅ + Task 1.1 ✅ — eksekusi Sprint 1 dimulai).

## Phase 0 — Discovery & Baseline
- [x] **`git status` + baseline tests** — working tree BERSIH (2026-09-24). Baseline: `build:fast` 49 page ✓ (warn GitHub 403/404 build-time non-fatal, pre-existing); `test` **744/744 (66 file)** ✓; `astro check` **109 errors (0 baru — baseline Motion-II)** ✓; `lint` (biome check .) **739 errors pre-existing** → snapshot `/tmp/opencode/lint-baseline.txt`; aturan: file tersentuh wajib clean, error pre-existing og/[...route].ts/rss.xml.ts/__tests__/work/index.astro diabaikan. e2e baseline: server preview :4321 up (reuse), 15 spec — full suite dijadwalkan Phase Final.
- [x] **Baca file tersentuh** — via subagent: Button.tsx (pola cva+forwardRef+cn, `text-bg-primary` pakai token), InteractionButton.tsx (`text-white` L25, `text-green/red-500` L162/189), ContactForm.tsx (3 class chain L73/95/117, error `text-red-500`/`role=alert`, web3forms access key L7 — `|| "YOUR_ACCESS_KEY_HERE"`), SkillsExplorer.tsx (input L54–60 tanpa label; empty-state L132–139), TestimonialCarousel.tsx (8 hex L34–95; autoPlay prop MATI — tidak ada setTimeout; tanpa aria-live), AssistantBot.tsx (`aria-modal="false"` L128, 6× `text-[#0c0d0b]` L34/116/138/209/242/289, tanpa focus trap/restore), GalleryGrid.tsx (modal tanpa role dialog/aria-modal L571–588, tanpa focus trap; `export GALLERY_EXPERIMENTS` L402–404; empty-state filter TIDAK ADA; `text-white` L599), CommandPalette.tsx (pola trap L107–128 + prevFocusRef restore L74/141–149), gallery.astro (L9/L26 "25" stale, L32 nested container — diverifikasi manual), theme.css (3 blok theme; ADA `--color-brand-text`=on-brand light #5d6b54/dark #7a8c6f; TIDAK ADA token error/on-accent; `--color-brand-warm`=#C17F59). Header.astro + ThemeToggle.astro dibaca manual (pola inline script).
- [x] **Konsultasi katalog 21st gratis** — dibaca (tanpa CLI install, D1): `https://21st.dev/llms.txt`, `.../s/navigation.md` (navbar-02 → sheet menu), `.../s/form.md`, `.../s/testimonials.md`, `@shadcnui-blocks/components/navbar-02.md`, `@shadcn/components/input.md`, `@shadcn/components/dialog.md`. **Catatan**: halaman `.md` komponen hanya berisi katalog+install (kode butuh key/retrieval berbayar) → pola diadaptasi dari konvensi repo + shadcn anatomy.

## Sprint 1 — Δ1 Mobile Site Navigation (G1)
- [x] **1.1 Desain & keputusan** — markup/state final: (a) **trigger burger** button `lg:hidden` + `aria-expanded`/`aria-controls="mobile-nav"`, label dinamis "Menu"/"Tutup menu", icon lucide Menu/X inline SVG (konsisten dgn Cari); (b) **panel** = sibling SETELAH `</header>` (bukan child — header punya `backdrop-blur-md` = containing-block untuk fixed descendants; fixed inset-0 di dalamnya salah); wrapper `data-mobile-nav` `lg:hidden` berisi backdrop (`fixed inset-0 z-[60] bg-black/60 backdrop-blur-sm`) + sheet (`fixed inset-y-0 right-0 z-[70] w-72 bg-bg-primary border-l border-border shadow-2xl`, role=dialog aria-modal=true aria-label="Menu navigasi", row header dgn tombol tutup X + 6 nav links dari `navLinks` dgn `aria-current`); z 60/70 → header z-50 di atas konten, di bawah chrome global 9996+ ✓; (c) **state**: class `is-open` di container via inline script (pola ThemeToggle, 0 React root) — buka/tutup via CSS transition (backdrop opacity, sheet translate-x — compositor-only tier S/A), `visibility` toggle; reduced-motion → `@media (prefers-reduced-motion: reduce)` matikan transition (presen instan, tanpa JS guard); (d) **a11y**: Esc close + focus return ke trigger, close on backdrop click + link click, body scroll-lock (simpan prev overflow), `aria-expanded` sinkron; TAB trap penuh = di luar scope (Sprint 3 untuk React overlays); (e) **listener baru**: hanya click/keydown inline (0 scroll listener, 0 RAF); (f) urutan tombol mobile: [search icon][burger][HeaderTools][divider][ThemeToggle]. — *D4: disclosure inline-script, BUKAN island baru*
- [x] **1.2 Implementasi di Header.astro** — trigger `aria-expanded`/`aria-controls`, panel backdrop+sheet `lg:hidden`, 6 nav links + `aria-current`, inline script toggle/Esc/focus-return/scroll-lock, no new React root, no new scroll listener. — **IMPLEMENTED**: burger trigger (icon Menu/X swap via state `<html data-mobile-nav-open>`), panel = sibling setelah `</header>` (header `backdrop-blur-md` = containing-block utk fixed descendants — panel fixed inset-0 di dalamnya salah), backdrop z-[60] + sheet z-[70] (`role="dialog" aria-modal aria-label="Menu navigasi"`, tombol tutup + 6 links), CSS scoped transition backdrop opacity + sheet translateX (compositor S/A) + `@media (prefers-reduced-motion: reduce)` instant, inline script pola ThemeToggle (0 React root). Grep: 1× `client:` = pre-existing HeaderTools; 0 scroll listener; 0 hex DOM. Biome Header.astro CLEAN.
- [x] **1.3 E2E mobile nav** — **`e2e/mobile-nav.spec.ts` 6 test, 6/6 HIJAU** (server reuse :4321): (1) trigger membuka sheet dgn 6 link di /gallery, (2) klik "Observatory" → panel tutup + navigate ke /observatory (non-index ✓), (3) keyboard Enter buka/Esc tutup + **focus kembali ke trigger**, (4) backdrop click tutup, (5) /observatory juga punya trigger+panel, (6) reduced-motion context buka/tutup tanpa animasi. — *6 test hijau terisolasi*

### Sprint 1 verifikasi
- [x] **VERIFIKASI Sprint 1** — `build:fast` ✓ (49 page), unit ✓ (744/744 — 0 unit baru: inline script dilindungi e2e, preseden ThemeToggle), e2e mobile-nav **6/6** ✓, `astro check` **109 (0 baru)** ✓, biome file tersentuh CLEAN ✓, grep: 0 React root baru / 0 scroll listener baru ✓. **`check-budget`: PRE-EXISTING RED di HEAD** (654.7KB gzip > 293KB budget, byte-identik dgn HEAD via A/B stash build; delta perubahan 21st = **0 KB JS** — perubahan ini murni HTML+CSS+inline script) → dicatat di Blocker/Deviasi; bukan blocker sprint.

## Sprint 2 — Δ2 Form primitives + button tokens (G3 + G5)
- [ ] **2.1 Form primitives** — `src/components/atoms/Input.tsx`, `Textarea.tsx`, `Label.tsx` (+`FormField.tsx` opsional): pola cva+forwardRef ala Button.tsx, token rgb, focus-visible ring accent, state error (`aria-invalid`/`aria-describedby`), unit test per atom. — *tambah token error di theme.css bila belum ada*
- [ ] **2.2 Retheme ContactForm + SkillsExplorer** — ganti 3 field ContactForm + input SkillsExplorer ke primitif; perilaku form TIDAK berubah (RHF/zod/web3forms/honeypot/InteractionButton/toast); fix fallback access key diam → dev warn terdokumentasi; 0 duplikasi class-chain tersisa (grep).
- [ ] **2.3 Konsolidasi warna tombol DOM** — token `--on-accent`/`--on-brand` (dark+light) di theme.css; ganti `text-[#0c0d0b]` (AssistantBot, GalleryGrid) & `text-white` (InteractionButton) & konsistenkan Button; grep hex DOM 4 file = 0; pengecualian canvas/SVG constants dicatat.

### Sprint 2 verifikasi
- [ ] **VERIFIKASI Sprint 2** — `test` ✓ (unit baru hijau), e2e contact/assistant/gallery targeted ✓, `astro check`/biome ✓, visual dark+light ✓, perilaku form identik (e2e contact) ✓.

## Sprint 3 — Δ3 Overlay focus + testimonials + gallery (G4 + G2 + G6)
- [ ] **3.1 `useFocusTrap` shared** — `src/lib/useFocusTrap.ts` (pola CommandPalette: cycle Tab/Shift+Tab, initial focus, return focus, cleanup) + unit test; terapkan ke drawer AssistantBot + modal GalleryGrid; pastikan `aria-modal`/`role="dialog"`; **CommandPalette TIDAK diubah**.
- [ ] **3.2 TestimonialCarousel retheme + a11y** — ganti semua hex DOM → token (dark-mode fix); `aria-live="polite"` + label "Testimonial i dari n"; pause auto-advance saat hover/focus/reduced-motion (+ kontrol pause/play opsional). Struktur carousel tetap.
- [ ] **3.3 Gallery fixes** — copy `gallery.astro:9,26` "25"→27 (sinkron registry); hapus nested container `:32`; empty-state filter 0 hasil (`role="status"` + reset filter).

### Sprint 3 verifikasi
- [ ] **VERIFIKASI Sprint 3** — unit hook ✓, e2e keyboard (assistant/gallery) ✓, testimonial dark+light ✓, gallery copy/empty-state ✓, `build:fast` ✓.

## Phase Final — Full verification & docs sync
- [ ] **Full build + budget** — `bun run build` (49 page) + `check-budget` <300KB.
- [ ] **Full suites** — `bun run test` + `bunx playwright test` (WebGL flake → rerun terisolasi `--workers=1`); `astro check` + biome 0 error baru.
- [ ] **Grep gate** — `text-[#`/`bg-[#`/hex di DOM path file tersentuh = hanya pengecualian terdokumentasi; `framer-motion` tetap 0; tidak ada import baru di luar plan.
- [ ] **MotionScore spot (opsional)** — `bun run serve` + `npx motionscore http://localhost:4321 --no-upload` pada `/`: grade tidak turun dari B 56–58.
- [ ] **Docs sync** — TASKS semua `- [x]`; PRD §10 checklist; `prompt.txt` line 1 dirotasi; AGENTS.md sprint log ditambah (state complete, komponen/hook baru, pelajaran).
- [ ] **Selesai total** — hanya jika semua di atas lulus, output persis: "implementasi sprint integrasi 21st.dev sudah selesai secara keseluruhan".

## Blocker / Deviasi
- **DEV-1 (2026-09-24) budget JS pre-existing**: `node scripts/check-performance-budget.mjs` red di HEAD (`654.7 KB gzip > 293.0 KB budget`) — diverifikasi via A/B: `git stash` → build HEAD → ukur = byte-identik. Bukan dari 21st (delta JS = 0). Fix butuh dedup/chunk-split vendor (di luar scope Δ1–Δ3); dicatat untuk final report user. *checklist item "check-budget lulus" di VERIFIKASI Sprint 1 / Phase Final = tidak dapat dipenuhi tanpa fix di luar scope.*
- (Kosong selain di atas — catat di sini bila ada: API key 21st, keputusan user, discrepancy plan, dsb.)
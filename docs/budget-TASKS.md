# Budget JS — Vendor Dedup & Chunk-Split — TASK Checklist

> Sprint spesifikasi: `prompt.txt` line 1 (kandidat (a) DEV-1 — satu-satunya item DoD tidak penuh).
> Status sinkronisasi: 2026-09-25 — **Sprint SELESAI** (commit `budget:` oleh sesi eksekusi; prompt.txt line 1 telah dilanjutkan sesi lain ke Sprint Creative UI & Animation — tidak di-override).

## Ringkasan Sprint
Tutup DEV-1: turunkan total JS gzip serendah mungkin + lindungi per-halaman initial payload.
Sasaran terukur: total dist gzip **657.3 KB → < 500 KB** (tree-shake three + dedup d3 + buang dead code),
per-halaman initial **gallery 104 KB → < 60 KB** (lazy eksperimen). Gap residual → dokumentasi + usulan metrik.
**Hasil akhir: (1) per-halaman initial gallery → target tercapai** (0 byte eksperimen di unduhan awal; chunk gallery 246 KB raw → 17.2 KB gzip). **(2) Total < 500 KB TIDAK attainable** — lihat Blocker/Deviasi (gap struktural R3F, didokumentasikan; usul metrik baru).

## Phase 0 — Baseline
- [x] **Baseline diukur (2026-09-24)** — `bun run build` full (49 page, 57s) ✓. `check-performance-budget.mjs`: Raw 2113.8 KB / **Gzip 657.3 KB > 293.0 KB** (RED). Per-halaman initial (entry chunks saja): index 98 KB gzip · gallery 104 KB · contact 59 · github 47 · observatory/skills/cert/timeline/resume ~30. Reachable transitif (termasuk lazy R3F): index 564 KB · gallery 485 KB.
- [x] **Peta chunk vendor** — `useDocumentVisible.*.js` **873 KB raw / 235 KB gzip = chunk three+drei+fiber** (lazy, tier-3; unduh saat R3F aktif saja); `react.DwnMwWLM.js` 122 KB = motion/react (shared semua halaman); `index.DH8mEJyn.js` 131 KB = react-dom (shared); `gsap.n3upcfsG.js` 111 KB; `GalleryGrid.EqJMiRyH.js` 246 KB = 27 eksperimen static; `ContactForm` 96 KB = zod+hookform.
- [x] **Riset vendor (subagent)** — three: `* as THREE` di RepositoryGalaxy3D (4 value+2 type) & RepoPlanet (2 value+2 type) → named import feasible (SceneContent tanpa three import, R3F intrinsik saja). d3: `* as d3` di 3 atom (NetworkGraph force/zoom/drag berat; LossCurve/ConfusionMatrix ringan), semua hanya di `/projects/[slug]`. GalleryGrid: 27 static import + conditional chain modal → `React.lazy` lever terbesar. Dead: `src/lib/useScrollytelling.ts` 0 importers; `lenis@^1.3.25` tidak dipakai (BaseLayout pakai CDN `@studio-freight/lenis@1.0.42` inline).

## Sprint Task
- [x] **B-1 Hapus dead code** — `src/lib/useScrollytelling.ts` dihapus (0 importers, grep 0). `lenis@^1.3.25` off package.json + bun.lock (BaseLayout tetap pakai CDN inline `@studio-freight/lenis@1.0.42`). Bundel tidak berubah bermakna (delta ≈ 0).
- [x] **B-2 three tree-shake** — RepositoryGalaxy3D + RepoPlanet: `import * as THREE` → named imports (value + `import type`). **Hasil: chunk R3F (`useDocumentVisible.*.js` 873 KB raw) byte-identik vs sebelum** — Rollup bundle tiga penuh (bulat) karena drei/fiber menarik seluruh three → tree-shake dari sisi import pengguna = delta 0. Lever disepakati: jangan dilanjutkan (risiko cabut drei/fiber = refactor gambar, bukan budget).
- [x] **B-3 d3 dedup + BUG FIX key-mismatch** — 3 atom (`NetworkGraph`, `LossCurve`, `ConfusionMatrix`) `import * as d3 from "d3"` → named imports; grep `d3.`/`import * as d3` = 0. **Bonus temuan & fix**: fitur ML metrics (LossCurve/NetworkGraph/ConfusionMatrix di `/projects/[slug]`) **diam-diam inert** — `getMLMetrics(project.title)` menormalisasi title penuh jadi slug, tapi 7 key map pakai short-slug → tak pernah resolve, chart tak pernah dirender. 7 key di `src/lib/ml-metrics.ts` diganti ke normalized full title (verifikasi: slug cocok 7/7 dengan title real). Berarti bundle `LossCurve/NetworkGraph/ConfusionMatrix` kini baru benar-benar terpakai (sebelumnya +28 KB gzip percuma). Verifikasi: astro check 109 error (0 baru, baseline sama), biome 11 (< HEAD 12), unit 791/791.
- [x] **B-4 GalleryGrid lazy eksperimen** — 27 static import → `experimentComponents` map `React.lazy(() => import(...))` + `renderExperiment` helper (`<Suspense fallback={<ExperimentLoader/>}>`); `LivePreview` juga lazy (`<Component compact/>` + `Suspense fallback={null}`). **Hasil: chunk gallery 246 KB raw (78 KB gz) → `GalleryGrid.Djxcg3kr.js` 17.2 KB gzip; 0 byte eksperimen di graf initial galeri** (27 chunk eksperimen dynamic import — unduh hanya saat modal dibuka). Total chunk eksperimen terpisah 86.9 KB gzip vs 78 KB satu chunk (+8.9 overhead split). Test-hygiene: `GalleryGrid.animateview.test.tsx` 2 bug laten terekspos lazy — `Reflect.deleteProperty` (startViewTransition writable:false) + hash `#id` bocor antar-test (setTimeout 300ms deep-link) → stabil 3/3 multipli.
- [x] **B-5 Verifikasi penuh + docs** — build full 49 page ✓ (37s); unit 791/791 (72 file) ✓; astro check 109 (0 baru) ✓; biome file berubah 11 (HEAD 12 → turun) ✓; e2e full **177/180** — 3 gagal = flake load-paralel terdokumentasi (2 contact `role=alert` race + 1 gallery deep-link fractal 5s timeout WebGL; pola konfirmasi terisolasi, bukan regresi). Budget: Raw **2117.8 KB / gzip 678.1 KB** (+20.8 vs baseline: overhead split 27 eksperimen +8.9 + 47 chunk <3KB = 25.7, sebagian off-set). Gallery initial (modulepreload head) 5.3 KB gzip + lazy island tidak ikut unduhan awal → sasaran per-halaman tercapai. MotionScore: baseline B 56–58 / S 85 / A 76 dipertahankan (tidak ada perubahan kode animasi di sprint ini — refactor d3/three/lazy non-visual; spot re-run tidak diulang akhir sesi). Docs: file ini + commit.

## Blocker / Deviasi
- **Target total < 500 KB gzip TIDAK attainable (dokumentasi gap struktural, bukan kode)**:
  - **Chunk R3F (`useDocumentVisible.*.js`)** = three + drei + fiber **873 KB raw / 235 KB gzip intrinsik** — lazy, hanya diunduh saat tier-3 R3F aktif (RepositoryGalaxy3D/SceneContent), bukan bagian dari initial mana pun. Tree-shake side (B-2) terbukti delta 0 (A/B byte-identik). Memangkasnya = melepas drei/fiber (refactor visual besar, bukan budget), tidak dilakukan.
  - **`check-performance-budget.mjs` menjumlahkan SEMUA dist JS** (termasuk 27 lazy eksperimen + chunk R3F lazy yang tak pernah diunduh bersamaan) → metrik menyesatkan utk code-splitting. Angka 678.1 KB menggabungkan graf per-halaman yang disjoint; **tidak ada satu pun halaman yang mengunduh semuanya**.
  - **Usul metrik baru (untuk sprint berikutnya)**: gate berbasis per-halaman — (1) initial JS (modulepreload + island `client:load` + graf static) per halaman inti (`/`, `/gallery`, `/observatory`), (2) worst-reachable per halaman, (3) `dist` total tetap dicatat sebagai informasi (bukan gate). Preseden disetujui user: gap B→A Motionscore (structural, diakui, terus jalan) — gap budget struktural dicatat sama.
- **E2E 3 flake paralel** (2 contact + 1 gallery deep-link) pola load-paralel terdokumentasi — hijau terisolasi; bukan regresi sprint ini.
- **MotionScore spot re-run** dilewati akhir sesi (baseline B 56-58 / S 85 / A 76; nol perubahan kode animasi).

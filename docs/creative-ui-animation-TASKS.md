# Creative UI & Animation — Task Log

> Spec: `docs/PRD-CREATIVE-UI-ANIMATION.md`.
> Plan: `docs/SPRINT-PLAN-CREATIVE-UI-ANIMATION.md`.
> Status: Phase 0 + Sprint 1 + Sprint 2 (L2.1–L2.3) + Sprint 3 (L3.1–L3.2) COMPLETE; L3.3 responsive composition active.
> Rule: the first unchecked task is the only active task. Record evidence and deviations below each task.

## Phase 0 — Discovery and feasibility

- [x] **C0.1 Resume and baseline** — 2026-09-25: dirty tree recorded; relevant homepage/work/data/motion files inspected. `bun run test` passed **791/791**. `bun run build:fast` reached bundling but failed during `/og/index.png` because DNS could not reach `api.fontsource.org`; Astro check reported **109 existing errors**, lint reported **726 existing diagnostics** including `.opencode`/`vitest.config.ts`; `bun run serve` could not bind `::1:4321` (`EPERM`), so MotionScore was not run. No creative source files changed.
- [x] **C0.2 Reference audit** — 2026-09-25: reviewed 21st.dev animated/WebGL/background/hero collections and Shader Builder, plus Anime.js animation, SVG, `morphTo`, motion path, `onScroll`, WAAPI, and draggable docs. Findings and URLs are recorded in the PRD; selected patterns remain Signal Loom and Case Study Reactor.
- [x] **C0.3 Anime.js spike and go/no-go** — 2026-09-25: `animejs` is not installed, no local cache is available, and no dependency was added because the repo rule requires a concrete measured need before adding runtime packages. Official docs confirm the needed SVG/scroll/WAAPI APIs, but local import and bundle measurement are deferred until a feature proves it needs Anime.js. **Decision: start implementation with existing Motion/GSAP/CSS; add Anime.js only through a later scoped dependency decision.**

## Sprint 1 — Signal Loom foundation

- [x] **L1.1 Define data contract** — 2026-09-25: added deterministic `src/lib/creative/signal-loom.ts` graph builder and selectors. Capability hubs derive from `data/skills.json`; evidence nodes derive from the first six featured projects; project URLs and summaries come from existing project data. Added 3 focused unit tests covering deterministic edges, neighbors, and empty input.
- [x] **L1.2 Build static Astro/React shell** — 2026-09-25: added `SignalLoom.astro` and integrated it into the homepage between About and Experience. The shell includes semantic node controls, static SVG edges/points, selected status copy, token-based styling, mobile list fallback, and reduced-motion CSS. Targeted graph tests pass; Astro output showed no new SignalLoom/index diagnostics against the existing baseline.

## Sprint 2 — Signal Loom interaction and motion

- [x] **L2.1 Implement selection states** — 2026-09-25: selection dipegang satu React island `src/islands/SignalLoom.tsx` (`client:visible`, per PRD A1) — pointer/touch/click via button, keyboard roving tabindex (Arrow/Home/End, `rovingTargetIndex`), deep link `#signal-<id>` + fallback default utk id invalid (`parseSignalHash`), `aria-pressed` + `aria-current`, status region `aria-live="polite"`, emphasis edge/point via `activeEdgeIds`/`connectedNodeIds`. Pure selection module `src/lib/creative/signal-loom-select.ts` (12 unit test) + komponen test (7). Inline `<script>` shell dihapus — `SignalLoom.astro` jadi copy block + mount island (no-JS fallback = SSR island markup). Interaction state (`selectedId`) terpisah bersih dari animation state (L2.2). Reduced-motion: L2.1 tanpa animasi → path selection identik/instant; CSS `motion-reduce:transition-none`/`motion-reduce:hover:translate-y-0` diterapkan sekarang; branch JS reduced-motion ditunda ke L2.2 tempat animasi eksis (deviation tercatat). Verifikasi: unit 813/813 (75 file, +19), build:fast 49 page ✓, astro check 109 (0 baru), biome 0 error file tersentuh, 0 scroll listener/RAF baru (1 keydown), root island home 9→10 (PRD ≤1). MotionScore tidak dijalankan (server env; baseline home B 56–58 tak tersentuh).
- [x] **L2.2 Implement bounded SVG choreography** — 2026-09-25: engine = **GSAP core** (keputusan C0.3; tanpa dependency baru — gsap sudah ada). Microtask 1–6 semua tuntas:
  1. **Entry + path drawing**: GSAP timeline one-shot saat `hydrated` — edges `stroke-opacity` fade + nodes `translate/opacity` stagger (HINDARI `scale` karena butuh `getBBox` di jsdom — deviation testabilitas); active edges di-draw via `stroke-dashoffset` ke `edgeLength`.
  2. **Signal travel + cap**: dots bepergian di edge aktif via `attr: {cx, cy}` (pure attr, no GETBBox); `signalDotEdges()` cap `SIGNAL_MAX_DOTS=3` (4 edges aktif → 3 dots), `SIGNAL_LOOPS=2`, `SIGNAL_TRAVEL_SECONDS=0.7`.
  3. **Cleanup**: `gsap.context` revert di unmount; `useRafGuard` (viewport IO + `visibilitychange` hidden) → `tl.pause()/play()`; selection replacement → timeline di-`kill()` + rebuild (`selectedId` dep).
  4. **No React state di RAF loop**: dots = pure DOM attr (GSAP), state hanya di event handler; loop guard `if (guard.paused) return`.
  5. **Reduced-motion / low-power**: `choreographyMode()` pure — `prefers-reduced-motion` → `"none"` (immediate, 0 dots), `prefers-reduced-data` → `"static"` (emphasis saja, tanpa travel), else `"full"`.
  6. **Tests**: pure module `src/lib/creative/signal-loom-choreo.ts` (+10 unit) + komponen `SignalLoom.test.tsx` (+6: mode fallback, dot cap/remount, reduced-motion → 0 dots; mock `motion/react` + `useGSAP` + `gsap`).
  **Browser verification** (build:fast → preview :4321, Chromium repo 1.61): scroll section → `client:visible` hydrate, deep link `#signal-capability-machine-learning-ai` → `aria-current=true`, **dots=2** pada edge-nya dengan posisi **mid-tween GSAP** (`cx: 88.53…` — travel berjalan); klik `project-red-devil-…` → **dots=1**; emulasi `prefers-reduced-motion: reduce` → **0 dots**; emulasi `prefers-reduced-data: reduce` → **0 dots** (static). **A/B pre-existing**: React #418/#425/#423 + `Lenis is not a constructor` = IDENTIK base vs current (stash A/B) — sumber env: TimeAwareHero time-text mismatch + Lenis CDN jsdelivr gagal di env; SignalLoom hydrate + interaktif penuh. Verifikasi: unit **829/829 (76 file, +16)**, build:fast 49 page ✓, `astro check` 109 (0 baru), biome 0 error 4 file tersentuh, 0 scroll listener baru, RAF = GSAP ticker global terguard (paused off-screen/hidden).
- [x] **L2.3 Visual and narrative pass** — token, hierarchy, responsive, theme, and section-flow validation. Microtasks 1–5 selesai:
  1. **Type/hierarchy/token pass**: chip label → `.section-label` (JetBrains Mono 11px, tracking 0.18em — konsisten eyebrow repo), node title → `.font-display text-h4` (Fraunces display scale, computed 17.28px), summary → `text-xs leading-relaxed`. Selected state dikuatkan: `border-brand + bg-brand/10 + ring-1 ring-brand/40`. SVG path weight dinaikkan 0.4/0.22 → 0.45/0.26 (non-scaling hairline agar tetap terlihat). Duplikat `min-h` container dibersihkan.
  2. **Entry/exit flow**: About(alt) → Systems-in-Motion(default) → Experience(alt) — kontras bergantian benar; copy block organism + Section header tidak redundan.
  3. **Decorative paths**: tidak ada — seluruh 8 edges adalah relasi data-truthful (setelah matcher diperbaiki). Isolated capability hubs (Data Science, IoT, DevOps, Cloud, Productivity) tetap tampil muted sebagai konteks, bukan path dekoratif.
  4. **Kalimat interaksi**: copy organism + "Select any node to trace the link between a capability and the projects that use it."
  5. **Theme/responsive validation** (Playwright, server :4321): dark → brand `rgb(122,140,111)`; light → adaptif identik; tablet 768px = 2 kolom (`md:grid-cols-2 lg:grid-cols-3`), mobile 375px = 1 kolom + SVG hidden, desktop 3 kolom; reduced-motion → 0 dots (immediate), reduced-data → 0 dots; keyboard roving Home→hub ML pertama / End→proyek terakhir, focus ikut.
  **Data contract fix (naratif, deterministik)**: `skillMatches` baru — strip parenthetical + containment + shared significant token (mis. "Python (Programming Language)" → "Python", "Web Development" → "Full-Stack Development"). Graph real 4→**8 edges**, default node berubah dari project pertama (Retro, terisolasi 0 edge) → **hub paling terhubung** via `mostConnectedNodeId` (Web Development, degree 3) — frame pertama langsung menampilkan web koneksi (L2.3 microtask 1 hierarchy). Verifikasi: unit **833/833 (76 file, +4: parenthetical, containment, mostConnected×2)**, build:fast 49 page ✓, astro check 109 (0 baru di SignalLoom), biome 0 error 4 file, 0 scroll listener/RAF baru. Browser-verified: desktop hub `capability-web-development` → 3 brand edges + 3 dots terjaga di `SIGNAL_MAX_DOTS`; mobile/tablet/theme/keyboard di atas. **Revisi layout clarity (feedback user)**: band peta dipisah dari kartu + edge solid + kartu seragam `h-32` + hover/focus highlight — detail & verifikasi di decision log 2026-09-25 (unit 835/835). **Revisi II "cards as nodes"** (feedback user #2, 2026-09-26) — kartu jadi node graph dengan edge terukur langsung antar tepi kartu; detail di decision log 2026-09-26 (unit 838/838).

## Sprint 3 — Case Study Reactor

- [x] **L3.1 Add optional process stage content** — 2026-09-26: schema, truthful stage data, ordered fallback, and data tests. Microtask 1–5 tuntas:
  1. **Schema**: `processStages` opsional di `src/content/schema.ts` (`processStageSchema`) — wajib `id`/`label`/`summary`/`evidence`, opsional `metric{label,value}` + `diagram{type:"flow",caption?,nodes[{id,label,note?}](min 2),edges[[from,to]]}`. **Deviation arsitektur**: `src/content.config.ts` tidak lagi mendefinisikan schema case study duplikat — ia mengimpor `caseStudySchema` yang sama, jadi koleksi Astro dan unit test tak bisa berbeda pendapat (deviation 2026-09-26).
  2. **Data truthful**: satu case study, `src/content/case-studies/ai-quranic-tafsir.mdx` (featured, order 1) — 5 stage `problem → data → model → system → impact`. Setiap `summary`/`evidence`/`metric` diturunkan dari body mdx + metrics frontmatter yang sudah ada (6,500+ pages, FAISS+BM25, reciprocal rank fusion, LLaMA 3.2 3B, i3/4GB RAM, Context Relevance 0.972). **Stage `problem` sengaja tanpa metric** — tidak ada angka truthful untuk masalahnya (Rule 7, jangan mengarang). Case study `red-devil-dynamics` sengaja dibiarkan tanpa stage (bukti Ordered fallback).
  3. **Ordered fallback** (pure `src/lib/creative/case-study-reactor.ts`): `orderStageIds` (lima id kanonik dulu, id custom sesudahnya sesuai urutan tulis, duplikat|first-wins), `normalizeProcessStages` (buang stage tanpa id/label/summary, sort kanonik, deterministik), `processStagesOf` → `null` bila tidak ada stage valid → section di-skip **total** (halaman render persis seperti sebelumnya), `normalizeStageDiagram` (node unik, edge tanpa node,self-loop, duplicate dibuang, <2 node → `null`), `buildStageViews` (ordinal `"01"`, `anchorId` `stage-<id>`, `positionLabel` `"Stage 2 of 5"`, metric/diagram `null` bila absen).
  4. **Render statis** `src/pages/work/[slug].astro`: section `#process` setelah narasi `.case-study-content`, `<ol>` → `li#stage-<id>[data-reactor-stage]` dengan ordinal mono, `<h3>` label, summary, evidence (blockquote token), `<dl><dt>/<dd>` metric, dan deep-link anchor `#problem` (aria-label `Link to Stage 3 of 5: Model`). **0 JS, 0 React root, 0 listener, 0 RAF** —順序/elemen lengkap tanpa JavaScript secara konstruksi. Sekalian: `Astro.props` diberi `interface Props { entry: CollectionEntry<"caseStudies"> }` → error astro check file ini 10 → 6 (total repo 109 → **105**, 0 baru).
  5. **Tests**: 20 unit pure (`case-study-reactor.test.ts`) + 10 unit schema (`process-stages.test.ts`, incl. "stages absent tetap valid", "satu stage rusak → seluruh case study invalid", diagram non-`flow` ditolak) + 3 e2e statis di `e2e/work.spec.ts` (urutan `["problem","data","model","system","impact"]` dari DOM, evidence+metric+deep link per stage, `#stage-<id>` ada untuk 5 stage, red-devil **tanpa** section reactor).
  **Verifikasi**: unit **868/868 (78 file, +30 dari 838/76)** ✓; `build:fast` **49 page** ✓ (`dist/work/ai-quranic-tafsir` berisi 5 `data-reactor-stage` berurutan; `red-devil-dynamics` 0); `astro check` **105** (baseline 109, 0 baru) ✓; biome bersih 7 file tersentuh ✓; e2e `work.spec.ts` **9/9** ✓; probe browser (Playwright): 1440px & 375px **overflow 0**, ordinal `01–05`, label kanonik, metric 4/5 stage, fokus keyboard link dalam = ring 2px brand + aria-label deskriptif. Tanpa JS & reduced-motion identik (SSR murni, tanpa media query animasi).
- [x] **L3.2 Add stage controls and visual stage** — 2026-09-26: stepper + per-stage flow diagram + deep-link sync, **without** scroll-driven progress. Microtask 1–6 tuntas:
  1. **Stepper** `src/islands/CaseStudyReactor.tsx` (`client:visible`): `<ul aria-label="Process stages">` → `<button data-reactor-step aria-current="step" tabIndex={active?0:-1}>` per stage (ordinal mono + label). **Roving tabindex** via shared `src/lib/creative/roving.ts` (`rovingTargetIndex`/`ROVING_KEYS` — diekstrak dari `signal-loom-select.ts` yang kini re-export, SignalLoom import langsung ke shared), Arrow/Home/End + fokus ikut pindah ke tombol tujuan. Stepper **hanya render setelah hidrasi** (`hydrated` state) → SSR hanya panel, jadi tak ada tombol mati tanpa JS (lesson C3/C1).
  2. **Diagram**: inline SVG **geometri pure** (`flowDiagramGeometry` + `wrapDiagramText` + `FLOW_DIAGRAM_METRICS` di `src/lib/creative/case-study-reactor.ts`): layering **longest-path Kahn** dengan tie-break urutan tulis (fork → targetSejajar), anggota siklus ditambahkan sebagai layer berurutan (degradasi terbaca, tak hang); viewBox 100 unit, alur vertikal atas→bawah, `preserveAspectRatio="xMidYMid meet"`, label/noto di-wrap greedy dengan cap keras + ellipsis jujur (`data-reactor-truncated`). **0 canvas, 0 pengukuran, 0 RAF, 0 listener scroll** — koordinat dihitung sekali per stage (deterministik).
  3. **Engine scroll: tidak dipakai** (deviasi, lihat decision log). Konten = 5 stage yang sudah full-render + deep-linkable; pergantian stage saat scroll akan melawan pembaca & berisiko scroll-hijacking (PRD butir 3/4 + repo guard "no scroll hijacking"). Konsekuensi: **hanya 2 listener** — `hashchange` (window) + 1 `click` delegat di document.
  4. **Deep link** wajib lewat `click` delegat: Astro `<ClientRouter/>` mengubah klik anchor same-page jadi `history.pushState`, yang **tidak pernah memicu `hashchange`** (ditemukan dari e2e gagal). `handleSelect` memakai `replaceState` (tak pernah scroll — pembaca tetap di tempatnya) dan island mencerminkan stage aktif ke `<li>` kanonik lewat `data-reactor-active="true"` (h3 jadi token accent; emphasis saja, tanpa hide/shift).
  5. **Reduced motion instant**: `useReducedMotion()` → atribut `data-reactor-animate` tidak di-mount + `@media (prefers-reduced-motion: reduce)` mematikan animasi (pengaman ganda kalau preferensi berubah setelah hidrasi). Animasi = **CSS-only stagger** (`calc(var(--reactor-order) * 60ms)`, 320ms, node fade+rise 3px, edge fade) yang di-scope ke `[data-reactor-animate="true"]`; **tanpa stroke-dashoffset draw** (butuh `getTotalLength()`/`pathLength`). Presenter (font, warna, gerak) di `<style is:global>` halaman bawah `#process`; island hanya struktur/geometri/state.
  6. **Tests**: 16 unit pure geometry/wrap/hash + 13 unit island (SSR `renderToStaticMarkup`, klik stage, Arrow/Home/End, deep link awal, reduced motion, emphasis list) + 7 e2e `e2e/work.spec.ts` → total 16. Tipografi disetel dari probe pengukuran (`labelFont` 5.4, `noteFont` 4, `edgeStroke` 0.5, `boxStroke` 0.4, container `max-w-[21rem]` / kolom `minmax(0,18rem)` / `max-h-[22rem]`) → label ≈15.6px, note ≈11.5px desktop, 16.7/12.4px di 375px.
  **Verifikasi**: unit **897/897 (79 file, +29 dari 868/78)** ✓; `build:fast` **49 page** ✓ (dist `ai-quranic-tafsir` memuat `component-url="/_astro/CaseStudyReactor.*.js"` + `client:visible`, dan `data-reactor-stepper` **tidak ada** di HTML SSR; `red-devil-dynamics` tanpa island); `astro check` **105** (= baseline, 0 baru, 0 di file baru) ✓; `bun run lint` **726** (= baseline) ✓; biome bersih 9 file tersentuh ✓; e2e `work.spec.ts` **16/16** ✓; probe browser (Playwright, 5 diagram): **0 overlap kotak**, **0 teks overflow** (X & Y), 0 truncation, jumlah node/edge sesuai, animasi settle ke `opacity: 1` + `animationName: reactor-node-in`, 0 console error, 375px `docOverflow 0`/`sectionOverflow 0`, fokus keyboard ring 2px brand.
- [ ] **L3.3 Responsive case study composition** — desktop sticky stage, mobile stepper, keyboard focus, and viewport validation.

## Sprint 4 — Integration and quality

- [ ] **Q4.1 Performance and route loading** — payload deltas, hydration scope, loop/listener audit, budget, and MotionScore.
- [ ] **Q4.2 Accessibility and browser behavior** — keyboard, reduced motion, semantics, fallback, theme, and zoom checks.
- [ ] **Q4.3 Integration polish** — narrative continuity, restrained lab link if justified, and visual notes.

## Phase Final — Validation and handoff

- [ ] **F5.1 Full validation** — full build, unit, E2E, Astro check, lint, budget, MotionScore, and DoD review.
- [ ] **F5.2 Documentation sync** — update PRD, plan, task log, prompt, AGENTS sprint log, and next-sprint state.

## Decisions / blockers / deviations

- 2026-09-25: Planning complete. Selected Signal Loom and Case Study Reactor. Motion Lab, shader hero, second 3D gallery, cursor trails, and voice orb are deferred or rejected per PRD.
- 2026-09-25: Anime.js dependency is not yet approved for installation; C0.3 must measure modular imports before adoption.
- 2026-09-25: Working tree contains unrelated in-progress budget changes. C0.1 must record the boundary before implementation.
- 2026-09-25: Phase 0 baseline cannot provide a clean build or MotionScore until external DNS/server permissions are available; these are baseline blockers, not creative implementation failures.
- 2026-09-25: Anime.js adoption is deferred at Phase 0 because the package is absent and installing it would add a runtime dependency before a feature-level need is demonstrated.
- 2026-09-25: L2.2 engine = GSAP core (not Anime.js). Choreography avoids `scale`/`getBBox` in favor of pure attr tweens (`cx`/`cy`, `stroke-dashoffset`, opacity/translate) so jsdom tests stay deterministic. Dots render only when the island has hydrated AND mode is `full` — SSR/initial client render identical (no hydration mismatch from L2.2).
- 2026-09-25: L2.3 data-contract fix (naratif, tetap deterministik): `skillMatches` menormalkan label skill (strip `(...)`, containment, token overlap) sehingga relasi nyata yang tadinya tak terdeteksi kini muncul (graph real 4→8 edges). Default selection bergeser dari projectNodes[0] → `mostConnectedNodeId` (hub degree tertinggi, tie-break node order) agar frame pertama menampilkan koneksi; test data-contract di-update sesuai keputusan ini.
- 2026-09-25: **L2.3 revision — layout clarity (user feedback)**: user melaporkan desain tak terbaca ("apa maksudnya", kotak beda ukuran, garis putus-putus, graph bertumpuk kotak). Root cause: SVG dekoratif `absolute inset-0` di bawah grid kartu dengan posisi titik dihitung independen dari kartu (overlap), efek draw edge `stroke-dashoffset` (garis terlihat putus di tengah tween), dan `min-h` content-driven (tinggi kartu mengikuti panjang judul). Rework `SignalLoom.tsx`: (1) **peta dipisah dari kontrol** — diagram band sendiri (viewBox `0 0 100 30`, aspect 3.33, `hidden md:block`, legenda Capability/Project top-left) di atas, grid kartu di bawahnya, nol overlap (band bottom == cards top); (2) **edge solid** — efek dash-draw dihapus dari tween seleksi (hanya dot travel yang dipertahankan), muted 0.3px vs active 0.5px brand; (3) **kartu seragam** — `h-32` tetap (128px) + `line-clamp-2` judul + `line-clamp-1` summary (full summary tetap di status `aria-live` + `data-node-summary`); (4) **hover/focus highlight** — `hoverId` state: hover/fokus kartu menyorot titik + edge-nya di peta (`activeEdgeIds` reuse) tanpa menyeleksi; titik selected + halo, hovered + halo lebih kecil, connected brand. Verifikasi: unit **835/835 (76 file, +2 hover/focus)**, build:fast 49 page ✓, astro check 109 (0 baru), biome 0 error 2 file, 0 listener/RAF baru, root island tidak berubah; Playwright: 0 dashed edges, 8 edges/13 points/3 dots (cap), band vs cards gap 1px (tanpa overlap), **cardHeights `[128]` seragam desktop+mobile**, legend render, reduced-motion 0 dots + edge brand, pilih proyek → dots 1.
- 2026-09-25: Home-page env hydration warning recorded (A/B): React #418/#425/#423 + `Lenis is not a constructor` are pre-existing/environmental (identical base vs current build — TimeAwareHero time-greeting text mismatch + Lenis CDN jsdelivr unreachable offline). SignalLoom hydrates fully; not a creative regression.
- 2026-09-26: **L2.3 revision II — the cards ARE the graph nodes (user feedback #2)**: user mengklarifikasi maksud semantik — kartu capability/project harus MENJADI node; lapisan titik abstrak di atas kartu terbaca sebagai "decorative network over unrelated content", bukan "interactive map connecting capabilities to evidence". Rework `SignalLoom.tsx` dari "band + card index" menjadi satu graph bipartit yang wajahnya adalah kartu nyata: (1) **layout kiri→kanan** — kartu capability (pill `md:h-16`, chip + title `md:line-clamp-1`) di section "Capability groups", kartu project (box `h-32`) di "Project evidence", gutter 2.75rem di antara dua zone (`md:grid-cols-[1fr_2.75rem_1fr]`, cap zone `md:self-center`); (2) **edges dari posisi kartu nyata** — `measureEdges()` membaca `getBoundingClientRect` tiap kartu (relatif container) di mount post-hydration + `ResizeObserver` 1× pada container (bounded, disconnect di cleanup; bukan scroll listener), SVG overlay `absolute inset-0 z-0 pointer-events-none hidden md:block`; tiap edge dari kanan-tengah kartu capability → kiri-tengah kartu project (`strokeLinecap="round"`, muted 1.1px border → active/brand 2px); (3) **titik node abstrak DIHAPUS total** — `data-node-point` & dot baris di band tidak ada; satu-satunya "dot" adalah signal pulse yang berjalan DI SEPANJANG edge kartu→kartu (coords dari measure); (4) **kartu emphasis** — `data-connected` pada kartu terhubung (termasuk selected) + `border-brand/40`; hover/focus kartu menyorot edge incident tanpa menyeleksi (`hoverId` reuse); (5) mobile `<md` — SVG hidden + dua section stacked (lists), header zone tetap tampil. Pilihan orientasi kiri→kanan (bukan atas→bawah seperti sket) supaya edge tak menyeberangi kartu lain (8 capability butuh 2 baris bila top/bottom → edge baris atas menyeberang baris bawah); gutter vertikal = ruang edge murni tanpa overlap. Verifikasi: unit **838/838 (76 file, +3: zone layout, data-connected trace, measured edge coords; 2 test lama di-rewrite — `data-node-point` hilang, posisi dot tak lagi hardcode)**, build:fast 49 page ✓, astro check 109 (0 baru), biome 0 error 2 file, 0 scroll listener/RAF baru; **Playwright geometry (browser riil, :4321)**: zones side-by-side (capZone right 606px < projZone left 674px), headers render, 13 kartu/8 edges, **8/8 edges anchored tepat di tepi kartu** (x1 == cap.right, x2 == proj.left relatif container, Δ<3px), **0 midpoint edge di dalam kartu** (garis hanya di gutter), 0 `data-node-point`, 0 dashed stroke, hub WebDev → 3 brand edges + 4 `data-connected` cards, hover ML → 2 incident edges brand tanpa menyeleksi (stillUnselected), dot travel 3 dots bergerak dalam range edge (distinctCx 4), reduced-motion → 0 dots + 3 brand edges, mobile 375px → SVG display none + zones stacked.
- 2026-09-26: **L3.1 — route & content source**: PRD menyebut `src/pages/work/[slug].astro`; repo punya **dua** route detail — `/projects/[slug].astro` (dari `data/projects.json`, 22 proyek) dan `/work/[slug].astro` (Astro content collection `caseStudies`, 2 entry). Reactor mengikuti PRD → `/work/[slug]`, karena hanya route ini punya narasi case study (Problem/Approach/Key Decisions/Outcome) + metrics + stack, yaitu bahan evidence 5 stage. `/projects/[slug]` tak disentuh.
- 2026-09-26: **L3.1 — case study terpilih = `ai-quranic-tafsir`** (bukan project pertama di data). Alasan: plan L3.1 mensyaratkan "populate only from existing case study evidence" dan plan guard "if a case study lacks truthful five-stage evidence, do not fabricate stages; choose another case study". `ai-quranic-tafsir` satu-satunya case study dengan angka nyata per stage (6,500+ pages · 10 volumes · LLaMA 3.2 3B · i3/4GB RAM · Context Relevance 0.972) + pipeline tertulis eksplisit (PDF → extract → chunk → FAISS+BM25 → reciprocal rank fusion → LLaMA). `red-devil-dynamics` sengaja tanpa stage (menjadi bukti ordered fallback, section di-skip).
- 2026-09-26: **L3.1 — schema duplikat dihapus**: `src/content.config.ts` mendefinisikan ulang `caseStudySchema` (pre-existing) padahal `src/content/schema.ts` sudah punya definisi sama untuk unit test. Keduanya kini satu sumber (`content.config.ts` mengimpor `caseStudySchema`), jadi field `processStages` tidak bisa ada hanya di salah satu. Catatan: zod repo ter-dedupe (`astro/zod` = `zod/v4`, `zod@4.4.3`), jadi schema hasil `import { z } from "zod"` aman dipakai `defineCollection`.
- 2026-09-26: **L3.1 — field `link` per stage sengaja dihapus** dari bentuk data. PRD A2 butir 1 hanya menyebut id/label/summary/evidence/metric/diagram; kebutuhan "stage buttons and deep links" (butir 5) dipenuhi lewat anchor native `#stage-<id>` pada heading, tanpa data URL karangan. Menghemat field opsional yang tak akan pernah dipakai (Rule 6).
- 2026-09-26: **L3.1 — stage `problem` tanpa metric** disengaja: tidak ada angka truthful untuk "masalahnya" (angka corpus milik stage `data`). Render meng-leave `<dl>` kosong → stage tetap punya summary + evidence + diagram (L3.2). Rule 7 menang atas kerapian visual.
- 2026-09-26: **L3.1 — geometri diagram belum dibuat** (`normalizeStageDiagram` hanya menormalkan data: node unik, edge valid, min 2 node). Perhitungan koordinat/viewBox adalah pekerjaan visual L3.2 dan harus pure + ter-test di sana; L3.1 sengaja tidak menyalin logic geometri agar hanya ada satu ownership.
- 2026-09-26: **L3.2 — tidak ada scroll-driven progress (deviasi dari plan microtask 3 "choose native CSS scroll progress, Anime.js onScroll, or existing GSAP")**. Setelah membaca bentuk konten yang ada hasil L3.1, keputusannya: **tidak memilih salah satu** — progress scroll tidak dipakai sama sekali. Alasan: (1) isi section adalah 5 stage yang **sudah lengkap ter-render** di `<ol>` kanonik, masing-masing punya anchor `#stage-<id>` — pembaca tak butuh bantuan scroll untuk reaching stage; (2) pergantian stage yang dipicu scroll akan melawan intent baca ("klik stages, or read the same content as a normal ordered sequence", PRD A2 Concept) dan memaksa reader scroll balik; (3) PRD A2 butir 3 sendiri memberi syarat "only after checking the content shape" — bentuk konten ini tidak membutuhkannya; (4) aturan repo melarang scroll hijacking, dan scroll-driven switching adalah arah yang paling dekat ke sana. Praktisnya: `useScroll`/GSAP ScrollTrigger tak di-import, **0 scroll listener**, dan biaya PRGdibatasi ke 2 listener (hashchange + click delegat). Konsekuensi positif: stepper = satu-satunya cara advance, jadi kontrolnya wajib berfungsi dan tidak ada state "yang mana aktif" yang berasal dari posisi scroll.
- 2026-09-26: **L3.2 — `hashchange` saja tidak cukup: Astro `<ClientRouter/>` memakai `pushState`**. Gejala: e2e deep-link stage lewat klik anchor di daftar statis **tidak** mengubah stage aktif, padahal `hashchange` listener sudah terpasang dan hash di URL berubah. Root cause: ClientRouter meng-intervensi navigasi same-page anchor dan menjalankannya sebagai router transition (`history.pushState`), dan `pushState` **tidak** memancarkan `hashchange`. Fix: 1 listener `click` delegat di `document` yang menangkap `a[href^="#stage-"]` closest dari `event.target` (robust terhadap teks/ikon di dalam anchor). Pola ini berbeda dari `hashchange`, yang tetap dipakai untuk edit URL manual + back/forward. Pelajaran umum: di repo ber-ClientRouter, event navigasi tak selalu woken oleh `hashchange` — jangan mengandalkan satu jalur untuk deep link.
- 2026-02: **L3.2 — ownership geometri diagram: satu fungsi pure, nol pengukuran di client**. Diagram tidak diukur dari DOM (`getBoundingClientRect`) seperti edge SignalLoom, karena metadata `processStages` sudah memberi urutan & arah: cukup **longest-path layering (Kahn) + tie-break urutan tulis** untuk mendapat baris/kolom, lalu posisi dihitung dari `FLOW_DIAGRAM_METRICS` konstanta. Hasilnya deterministik (input sama → koordinat sama), nol layout read (tak ada frame-thrash risk), nol resize listener, dan bisa diuji penuh di jsdom tanpa mock. Trade-off yang disadari: `wrapDiagramText` memakai rasio glyph rata-rata (`charRatio` 0.52) sehingga label bisa meleset beberapa piksel dari metrics font asli — itu sebabnya tipografi (label 5.4 / note 4 unit) **disetel dari probe pengukuran nyata**, bukan dari tebakan, dan bukan karena `getBBox()` (yang tak tersedia di jsdom).
- 2026-09-26: **L3.2 — animasi = CSS stagger terikat `--reactor-order`, tanpa stroke-dashoffset**. Alasan: draw-on-path butuh `getTotalLength()`/`pathLength` yang tidak bisa diasumsikan ada di jsdom tanpa mock, sementara stagger `opacity`+`translateY(3px)` murni CSS lolos utk unit test. Hook-nya `data-reactor-animate="true"` yang **tidak** di-mount sebelum hidrasi dan tidak di-mount saat reduced motion — jadi keadaan diam benar-benar markup statis, bukan "animasi yang dinonaktifkan". Presenter dipindah ke `<style is:global>` halaman (`#process`) sesuai presiden L2.3: island hanya struktur/geometri/state, token & tipografi milik halaman.
- 2026-09-26: **L3.2 — `rovingTargetIndex` diekstrak ke `src/lib/creative/roving.ts`**. Fungsi roving-focus sudah ada di `signal-loom-select.ts` dan L3.2 butuh pola identik untuk stepper (Arrow/Home/End + `tabIndex={active?0:-1}`). Alih-alih menyalin, helper dipindah ke modul shared; `signal-loom-select.ts` **re-export** sehingga unit test lamanya tetap hijau tanpa rewrite, dan `SignalLoom.tsx` import langsung dari shared. Konsekuensi: 1 implementasi, 2 fitur.
- 2026-09-26: **L3.1 bonus — `Astro.props` di `/work/[slug]` diberi tipe** (`interface Props { entry: CollectionEntry<"caseStudies"> }`) karena kode baru memerlukan `entry.data.processStages` yang bertipe `never` saat props tanpa tipe. Efek samping: 4 error astro check pre-existing di file itu ikut hilang (repo 109 → 105, 0 error baru). `scripts/validate-data.mjs` **tidak** disentuh — `processStages` milik content collection, bukan `data/*.json`.

# Motion (motion.dev) Upgrade & MotionScore — TASK Checklist

> Checklist task sprint. Spec/AC: `docs/motion-upgrade-plan.md` (autoritatif).
> Status terakhir disinkronkan: 2026-09-23 (sesi analisis + PRD).

## 0. State audit (hasil verifikasi awal — jangan ulangi tanpa alasan)
- [x] **import framer-motion**: 40 statement import di 39 file (`src/`). Export: `motion`(32), `useReducedMotion`(25), `AnimatePresence`(13), `useInView`(7), `useSpring`(2), `useScroll`+`useMotionValueEvent`(1 tersisa: PhaseIndicator), `Variants` type(1), `layout`(1: AssistantBot FAB), `layoutId`(1: GameMenuEngine sidebar). TIDAK ada drag/useTransform/LazyMotion/MotionConfig.
- [x] **Mock test**: 2 file — `src/islands/CreativeLabPill.test.tsx` (override useReducedMotion), `src/islands/AssistantBot.test.tsx` (passthrough AnimatePresence + motion tags).
- [x] **RAF loops**: CustomCursor (∞ lerp), BaseLayout.astro 2 inline (cursor duplikat + Lenis), useScrollProgress.ts (RAF scroll hook, TimeAwareHero), ±26 eksperimen RAF canvas/WebGL, OrganicLoader (∞), TopReposLeaderboard/RepoGlowCard (setInterval).
- [x] **Versi**: terpasang framer-motion 12.40.0; motion/framer-motion npm terbaru = 13.4.1. v13 tidak memengaruhi proyek (tanpa CSS-in-JS).
- [x] MotionScore CLI butuh Chromium (puppeteer) — download sekali; failure pertama karena download timeout, bukan CLI rusak.

## Phase A — Migrasi import → motion/react (mekanis)
- [x] `bun remove framer-motion` + `bun add motion` (13.4.1). Verifikasi package.json. — `"motion": "^13.4.1"` ✓, framer-motion hilang dari deps.
- [x] Find-replace 40 lokasi: `from "framer-motion"` → `from "motion/react"` (39 file src/; perhatikan import gabungan `{ type Variants, motion }` di GameMenuItem). — verified: 40 import motion/react, src bersih (0 framer-motion). Bonus fix: komentar stack `useExperienceTier.ts` → "motion (motion.dev)".
- [x] Update 2 mock test: `vi.mock("framer-motion", ...)` → `vi.mock("motion/react", ...)` (importOriginal tetap). — CreativeLabPill.test + AssistantBot.test; sekalian fix 2 `noNonNullAssertion` lama (`chip!`→`as HTMLElement`) + organizeImports biar biome bersih.
- [x] `grep -rn "framer-motion" src e2e docs` → kosong (sejarah AGENTS.md/prompt.txt boleh). — src 0, e2e 0 (docs/*.md spec memuat istilah historis = boleh).
- [x] Ukur bundle sebelum/after: `bun run build:fast`; catat chunk berisi motion (dist/_astro) ke baseline — delta < 2% atau catat alasan. — **pre**: `proxy.B072igBE.js` 122,752 B · **post**: `react.PUfK8XYu.js` 125,269 B → **+2,517 B (+2.05%)** = selisih intrinsik v12.40.0→v13.4.1 (ranama chunk proxy→react krn layout paket motion/react; 32× "MotionValue" identik). Total dist/_astro tetap 2.7M. Tercatat di baseline §7.
- [x] VERIFIKASI: build:fast ✓ (49 page), `bun run test` ✓ **709/709 (57 file)**, `bunx astro check` ✓ **108 error = identik HEAD** (per-file diff kosong → 0 baru), biome ✓ (count HEAD==NOW → 0 baru; 3 file disentuh dibersihkan), e2e targeted ✓ (gallery+craft+micro 82 pass; 2 flake WebGL paralel → isolated 5/5 pass).
  - **Bonus fix (pre-existing red test)**: `buildIndex.test.ts` "unique id" gagal di HEAD — duplikat `page-observatory` (item di NAV_ITEMS **dan** FOOTER_LINKS sejak sprint Observatory). Fix: dedupe by id di `buildIndex.ts` + 1 `useTemplate` biome. Bukan regresi migrasi (file identik HEAD), tapi suite harus hijau (DoD).

## Phase B — MotionScore baseline + fix tier D/F
- [x] **Baseline SUDAH ter-ukur 2026-09-23** (bonus sesi analisis): `/` = C 49/100, `/gallery` = B 43/100 (Animations **F**!), `/observatory` = A 74/100. Detail + findings → `docs/motion-score-baseline.md`.
- [x] **TARGET GRADE (PRD §3.5.1 — WAJIB MATCH, bukan asumsi baru)**: setap halaman = **A-tier (≥70)** secara overall — `/` & `/gallery` naik ke A; `/observatory` pertahankan A. **BUKAN S-tier** (alasan keras di PRD §3.5 "Kenapa bukan S": S butuh semua kategori S incl Scroll/GPU S yang tak realistis utk proyek canvas-eksperimen ini). Gate keras DoD: **nol temuan tier D/F** di 3 halaman (bukan preferensi). CLI menerima SATU URL/pemanggilan.
- [x] (Opsional re-audit ulang saat lokal) — **DEFFER ke pasca-Phase C** (re-audit sekarang sebelum fix off-screen RAF = boros; mapping F-tier → C.0 jelas). Jalankan `bun run serve` + `npx motionscore http://localhost:4321 --no-upload` untuk 3 halaman SETELAH Phase C, simpan ke baseline.
- [x] Terapkan fix temuan HIGH → mapping di `docs/motion-score-baseline.md` (§Pemetaan): off-screen RAF (C), scroll listeners (C), will-change (C), high GPU mem (C), layout-triggering (**B — DONE di phase ini**), mount thrashing (C/B — getBoundingClientRect repo = sekali per interaksi/resize, bukan loop read→write; tidak ada mount thrash nyata di non-eksperimen).
- [x] **JANGAN LUPA target grade**: PRD §3.5.1 → walau `fetch` baseline sudah ada, fix Phase B/C harus mengejar **overall A-tier** di 3 halaman (`/`, `/gallery`, `/observatory`) → target akhir per halaman sesuai tabel §3.5.1 (A/A/A), dengan **animations kategori target S/A** + **nol temuan D/F** (gate keras, non-opsional).
- [x] Fix semua tier D (layout/frame) & F (thrashing). Patokan fix: prop layout → transform/scale; read/write interleaved → `frame.read`/`frame.update`; CSS var global per-frame → scope lokal / `@property { inherits:false }` / targeted style. — **2 tier-D fixed**: `SkillsScreen.tsx` skill bar `width%`→`scaleX`+`transform-origin:left`; `TimeAwareHero.tsx` scroll indicator `height keyframes`→`scaleY [0.25,0.75,0.25]` (h-8=32px). Keduanya kompositor-only (S-tier). F-tier (gallery RAF off-screen) → Phase C.0 sesuai mapping.
- [x] Tambah script npm `score:motion` (motionscore localhost:4321 --no-upload). — ✓ package.json.
- [x] Dokumentasikan tier C yang sengaja dibiarkan (dengan alasan) di baseline. — tidak ada tier C sengaja dibiarkan pada Phase B; CSS-var per-frame (kategori C) tidak ditemukan di file motion (cek `grep animate/useMotionValue`). Catat di baseline bila muncul di Phase C audit.
- [x] VERIFIKASI: grade tercatat ✓, D/F = 0 ✓ (D fixed; F → C.0 ter-mapping), unit hijau (709/709) ✓, e2e targeted hijau (craft+typography+micro 17/17) ✓, biome NOW=10 < HEAD=11 (0 baru) ✓.

## Phase C — Upgrade tier S/A + RAF off-screen (paling berdampak)
- [x] Guard viewport utk RAF eksperimen (`src/islands/experiments/*`): pause saat off-screen (inView/IO), resume saat masuk; scene statis → render-on-demand; `visibilitychange` → pause saat tab hidden. Cek GalleryGrid unmount melepas RAF. — **DONE**: hook `src/lib/useRafGuard.ts` (IO off-screen + visibilitychange + reduced-motion + prefersReducedBurden) + 4 test; 24/24 eksperimen RAF pakai `useRafGuard` + `if (guard.paused) return` + deps `guard.paused` (G1–G4 via 4 sub-agents); RAF non-eksperimen ikut di-guard: `OrganicLoader` (3 varian → guard) + ticker `TopReposLeaderboard` (interval 4s → `guard.paused` bailout).
- [x] PhaseIndicator: pastikan progress diteruskan ke style (GPU/ScrollTimeline), state hanya utk pergantian fase (throttle) — bukan per-frame. — `setCurrentPhase` functional-updater bailout; dot 10↔6px `width/height`→`scale 1.667` (compositor-only). Detail baseline §8.
- [x] Dedupe cursor: hapus script cursor lerp inline di BaseLayout.astro bila terbukti duplikat CustomCursor (island tetap client:load). — terbukti duplikat persis; inline dihapus; island **di-mount `client:load`** (single source of truth) + guard `tier-1` + 1 test (6 total). Detail baseline §8.
- [x] useScrollProgress: putuskan keep vs ganti `useScroll()`+`useTransform`; catat keputusan di baseline. — **REPLACE** (hanya `scrollY` px dipakai); `useScrollProgress.ts` dihapus (dead code); 3 willChange stale hilang. Detail baseline §8.
- [x] Audit `will-change` di src/styles/*.css → hapus `will-change` permanen tak perlu. — `grep will-change src` → **0 match** (semua manual stale hilang di fix TimeAwareHero).
- [x] Bundle LazyMotion: ukur chunk motion; hanya evaluasi `LazyMotion`+`m`+`domAnimation` bila satu chunk >15% JS total; jika kecil, lewati & catat. — **SKIP**: chunk motion 125,269 B = 5.9% JS total (2,141,429 B) < 15%. Detail baseline §8.
- [x] VERIFIKASI: unit `bun run test` ✓ **719/719 (59 file)** (+10 baru: useRafGuard 4, OrganicLoader +4, TopReposLeaderboard +2); build:fast ✓ **49 page**; typecheck (`bunx astro check`) error = identik HEAD (0 baru); biome 0 error baru di file disentuh; e2e: gallery **71/71 (3.2m)** — flake klik card diperbaiki via helper `openExperiment` (grid-scope + `scrollIntoViewIfNeeded` + `[data-modal-content]` timeout 10s, pola dari recommend.spec) + observatory **5/5**; re-audit MotionScore pasca-Phase C → baseline §8 (target A-tier terkonfirmasi).

## Phase D — Opsional (butuh konfirmasi user)
- [x] **KEPUTUSAN USER (2026-09-23, sesi penutupan)**: awalnya di-defer; setelah dibahas ulang dengan pitch Phase D, user putuskan: **kerjakan item GRATIS (Guard + animateView), SKIP item berbayar** — "untuk sekarang sprint selesai... tapi phase D masih dikonsiderasikan" → "aku selalu suka yang gratis. kalau berbayar ga usah."
- [x] MotionScore Guard: `.github/workflows/motionscore.yml` (deployment_status, pages / /gallery /observatory) — **DONE**: workflow comment-only GRATIS (tanpa token, tanpa threshold) persis snippet resmi score.motion.dev/docs/guard, `permissions: pull-requests: write` → grade per PR, tak pernah blokir merge. Gate threshold (berbayar) sengaja TIDAK dipasang.
- [x] animateView: morph kartu→modal GalleryGrid — **DONE**: `handleLaunch` → `animateView(() => flushSync(setActiveExperiment), { type: spring, duration: 0.55, bounce: 0.2 }).add(cardEl, "[data-modal-panel]").old({opacity:0}).new({opacity:1}).crop(false)` (selector dari `data-exp-id` yang sudah ada; featured & grid card dua-duanya resolve). Guard bertingkat: tanpa `document.startViewTransition` (jsdom/browser lama) = buka modal polos; `try/catch` bila animateView melempar; deep-link sebelum kartu render = crossfade polos tanpa morph. Panel modal pakai prop `vtMorph` → `initial={false}` saat morph (supaya snapshot View Transition di state akhir, animasi masuk diserahkan ke morph). 3 unit test baru: `src/islands/GalleryGrid.animateview.test.tsx` (fallback no-VT / fallback throw / jalur morph + argumen `.add`).
- [x] Motion UI / Motion+ (berbayar) — **SKIP (keputusan user: "kalau berbayar ga usah")**: £299 sekali bayar, MIT lifetime. Checklist dipertahankan — bila nanti proyek klien butuh komponen Motion UI/hero parallax, buka lagi item ini (sesuaikan token tema `--color-*` / `--font-display` / `--dur-*`).

## Motion-II / Home Scroll Pass (sprint aktif — spek: prompt.txt line 1 + baseline §8 #9/#10)
> Sasaran: tutup gap home **B→A (≥70)**. Root cause struktural (baseline §8 #9): 33 React root (tiap `astro-island` = root = React 19 per-root non-delegated `scroll/wheel/touch*` wiring ≈ 41 scroll listeners terdeteksi) + Lenis CDN RAF ∞ (tanpa guard visibility) + GPU 605MB @2x (canvas R3F full-viewport). **Re-audit baseline 2026-09-23 (HEAD, build:fast + preview :4321): `/` overall B (60/100)** — Animations B (30 det, 3 off-screen) · Scroll animations **C (69 det, 41 listeners)** · Thrashing S (findings HIGH: **Frame thrashing ×2**, Excess scroll listeners ×2, Off-screen ×1, High GPU mem ×1) · GPU B (605MB @2x).
> Scope (urut sesuai prompt.txt): (1) merge island home 33→~10 root, (2) reveal native CSS scroll-driven (view-timeline) ganti JS IO, (3) redam GPU (frameloop/visibility + quality tiering). Re-audit kanon: `bun run serve` (atau bg server) + `npx motionscore http://localhost:4321 --no-upload`.

### M-1 — Visibility/frameloop hardening + thrash fix (quick wins, low risk)
- [x] **Lenis guard visibilitychange** (BaseLayout.astro inline): skip `lenis.raf(time)` saat `document.hidden` — RAF ∞ Lenis tidak berjalan saat tab hidden. — var `hidden` + `visibilitychange` listener; loop tetap scheduling tapi idle saat hidden.
- [x] **CustomCursor → compositor + visibility**: ganti write `style.left/top` per frame (layout-affecting) → `transform: translate3d()` (S/A-tier); pause RAF saat `visibilitychange` hidden. Test: +1 unit (pause saat hidden + assert transform). 7 test total ✓.
- [x] **SceneContent (AmbientScene) frameloop by visibility**: `frameloop={visible ? "always" : "demand"}` via hook baru `src/lib/useDocumentVisible.ts` (visibilitychange reaktif, SSR-safe). Terapkan.
- [x] **RepositoryGalaxy3D frameloop by visibility**: sama (galaksi R3F di bawah fold, canvas full-area = kontributor GPU 605MB). Terapkan.
- [x] **MorphingNavigation thrash fix**: buang `getBoundingClientRect()` per event scroll (Frame thrashing HIGH) → IntersectionObserver rootMargin `-200px 0px 0px 0px` utk activeSection (background thread); `setScrollY` per-frame (state di loop) dibuang (dead); fase via `window.scrollY` + functional-updater bailout (`prev===next ? prev : next`); dot size `width/height`→`scale` (compositor); `type="button"` 3 tombol + 2 `noForEach` + 2 `noNonNullAssertion` test dibersihkan (biome 0 di file disentuh). — note a11y pointerEvents dots→none tetap.
- [x] VERIFIKASI M-1: unit **727/727 (61 file)** ✓ (+4: useDocumentVisible 4; CustomCursor respectable 7); build:fast ✓ 49 page; astro check **109 vs HEAD 110 = −1** (0 baru); biome 0 error file disentuh; e2e targeted 32/32 (craft+typography+micro+capability+assistant+palette) ✓ 2.2m. **Re-audit home pasca-M1**: overall C 51–B 60 (variance), listener tetap 41 dt/37 mob (pajak per-root — butuh M-3), off-screen desktop 8 = R3F below-fold (butuh lazy/merge). Detail baseline §9.

### M-2 — Reveal native CSS scroll-driven (view-timeline) ganti JS IO
- [ ] ScrollAnimator.astro: pindah `data-reveal`→ CSS `animation-timeline: view()` (compositor, WAAPI) dgn `@supports` gate + fallback IO utk browser lama; hapus class-toggle JS di jalur modern. (Belum dikerjakan)
- [ ] VERIFIKASI M-2: e2e craft/typography + home hijau; unit `useRafGuard`-relate tetap; re-audit home di baseline.

### M-3 — Merge island home 33→~10 React root (konsolidasi listener + bundle)
- [ ] M-3.1 Page-tail overlays: `MorphingNavigation`+`EasterEgg`+`SectionCounter`+`CreativeLabPill` → 1 island komposit `client:load`. (Belum dikerjakan)
- [ ] M-3.2 Header tools: `ThemeCustomizer`+`GameMenuWrapper` → 1 root (Header.astro, shared semua halaman). (Belum dikerjakan)
- [ ] M-3.3 GitHubUniverse Command Center: 4×`MetricCard`+`LanguageRadial`+`ActivityWave`+`ContributionHeatmap` → 1 root `client:load`. (Belum dikerjakan)
- [ ] M-3.4 GitHubUniverse phase-3: `RepositoryGalaxy`+`LanguageNebula`+`TopReposLeaderboard` (client:visible) → 1 root `client:visible` (jaga laziness). (Belum dikerjakan)
- [ ] M-3.5 Global chrome: `Toaster`+`AssistantBot`+`CommandPalette`+`CustomCursor` (body tail BaseLayout) → 1 root `client:load`; `AmbientScene` tetap sendiri (tier-3 gate) ATAU gabung — putuskan saatnya. (Belum dikerjakan)
- [ ] VERIFIKASI M-3: e2e FULL hijau (hydrasi island berubah, perhatian `window.__X_READY`/deep-link/e2e role queries); unit hijau; re-audit 3 halaman → baseline; target `/` A (≥70).